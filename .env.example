# API
DATABASE_URL=postgresql://comic:comic@localhost:5432/comictracker
JWT_SECRET=change_me_in_production_secret
COOKIE_SECRET=change_me_in_production_cookie
API_PORT=3001
WEB_ORIGIN=http://localhost:3000
COOKIE_SAMESITE=lax
# Optional: set when serving API on a separate subdomain (e.g. .example.com)
# COOKIE_DOMAIN=.example.com
# Optional override, default is true in production
# COOKIE_SECURE=true
NODE_ENV=development

# Web (optional)
VITE_API_URL=
API_UPSTREAM=http://api:3001
